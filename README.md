# Forms
## Follow instructions to install required modules
    python3 -m venv venv
    . venv/bin/activate
    pip install flask
    pip install flask_sqlalcehmy
Now run the file:

    python3 __init__.py
